import { Flex, Container } from "@chakra-ui/react"

import { ThumbProps } from "../types"


export const MovieDetailsCard = (props: ThumbProps) => {
  return(
    <Container>
      <Flex>
        
      </Flex>
    </Container>
  )
}